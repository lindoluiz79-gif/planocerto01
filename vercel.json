{
  "buildCommand": "npm install --legacy-peer-deps && npm run build && node scripts/generate-index.js",
  "framework": null,
  "installCommand": "npm install --legacy-peer-deps",
  "outputDirectory": "dist/client",
  "env": {
    "NODE_VERSION": "18"
  }
}
